{
    "version": 2,
    "builds": [
        {
            "src": "dir/main.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/api/main.py"
        }
    ]
}